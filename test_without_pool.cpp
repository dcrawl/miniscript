#include "MiniScript-cpp/src/MiniScript/MiniscriptInterpreter.h" 
#include <iostream>

using namespace MiniScript;

int main() {
    std::cout << "Testing basic interpreter without Context pooling\n";
    
    // Use the existing miniscript executable to verify it works
    std::cout << "Let's test with the built executable first...\n";
    
    return 0;
}